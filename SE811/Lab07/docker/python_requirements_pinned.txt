# Python 2.7 dependency set for Codeface, pinned to the last releases that
# still publish py2-compatible artifacts.
#
# Upstream python_requirements.txt / setup.py list these unpinned, which no
# longer resolves in 2026: pip would pick py3-only releases (or, for `VCS`,
# an unrelated PyPI squatter -- see report). Every pin below is the newest
# version whose Requires-Python still admits 2.7.

# --- core runtime -----------------------------------------------------------
PyYAML==5.4.1
progressbar==2.5
python-ctags==1.0.6
MySQL-python==1.2.5

# --- JIRA client (imported unconditionally by codeface/conway.py, which
#     project.py imports at module scope, so it is needed even for `run`) ----
jira==2.0.0

# --- helpers used by codeface.util / the test-suite ------------------------
subprocess32==3.5.4
mock==3.0.5
pytest==4.6.11

# --- transitive pins: newest py2-compatible releases ------------------------
requests==2.27.1
urllib3==1.26.20
idna==2.10
chardet==4.0.0
certifi==2021.10.8
six==1.16.0
python-dateutil==2.8.2
